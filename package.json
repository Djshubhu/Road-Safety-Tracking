{
  "name": "road-maintenance-complaint-tracking-system",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute road-maintenance-db --local --file=migrations/0001_init.sql",
    "db:remote": "wrangler d1 execute road-maintenance-db --remote --file=migrations/0001_init.sql"
  },
  "devDependencies": { "wrangler": "^4.34.0" }
}
